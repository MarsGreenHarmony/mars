<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.himedia.sn.ecommerce5th.mars.greenharmony.backoffice.login.mapper.LoginMapper">

	<select id="loginProc" parameterType="java.lang.String" resultType="ManagerDto">
		SELECT
			SEQ_MNG
			, ID
			, PASSWD
			, MNG_NM
			, TC_STATE
		FROM
			MANAGER
	</select>
</mapper>